{
  "name": "panda",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "bunx prettier . --check",
    "format:fix": "bunx prettier . --write",
    "lint:fix": "bunx stylelint public/css/*.{css,scss} --fix"
  },
  "dependencies": {
    "hono": "^4.8.5",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2",
    "stylelint": "^16.22.0",
    "stylelint-config-standard": "^38.0.0"
  }
}