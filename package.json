{
  "name": "panda",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "bunx prettier . --check",
    "format:fix": "bunx prettier . --write"
  },
  "dependencies": {
    "hono": "^4.8.5",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  }
}